name: Run unit test of feature branches

on:
  push:
    branches:
      'feature/*'

jobs:
  run-unit-tests:
    runs-on: ubuntu-latest

  steps:
    - name: Checkout
      uses: actions/checkout@v4.0.0
      
    - name: Run unit tests
      run: mvn test
